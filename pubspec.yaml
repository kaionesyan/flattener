name: flattener
description: Flatten and unflatten maps easily using this package. It exposes two methods with the names described previously. Currently only maps of type Map<String, dynamic> are supported.
version: 0.0.1+4
homepage: https://github.com/kaionesyan/flattener

environment:
  sdk: '>=2.8.1 <3.0.0'

dev_dependencies:
  pedantic: ^1.9.0
